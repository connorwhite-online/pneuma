# Pneuma wake-island Zephyr config (scaffold — expand during bring-up).
CONFIG_GPIO=y
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# PDM mic for wake-word capture.
# CONFIG_AUDIO=y
# CONFIG_AUDIO_DMIC=y

# BLE for one-time provisioning (see docs/PROVISIONING.md).
# CONFIG_BT=y
# CONFIG_BT_PERIPHERAL=y

# Low-power idle (enable once the loop is IRQ-driven).
# CONFIG_PM=y
# CONFIG_PM_DEVICE=y
